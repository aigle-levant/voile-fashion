// import { fetchSeedData } from "./fetchSeed";
// import { type GalleryCardProps } from "@/types/components";

// export async function fetchCollections(): Promise<GalleryCardProps[]> {
//   const seedData = await fetchSeedData();

//   const seedAsGallery: GalleryCardProps[] = seedData.map((p) => ({
//     id: p.id,
//     title: p.title,
//     image_url: p.image_url,
//     category: p.category,
//     culture: p.culture,
//     period: p.period,
//     material: p.material,
//   }));

//   //   const metData = await fetchMetObjects();

//   return [...seedAsGallery];
// }
